import React from 'react';

export const BackButton: React.FC = () => {
  return null;
  // const dispatch = useDispatch<AppDispatch>();

  // const handleClick = useCallback(() => {
  //   dispatch({ type: 'PREVIOUS_PAGE' });
  // }, [dispatch]);

  // return (
  //   <SecondaryButton onClick={handleClick} tabIndex={-1}>
  //     Back
  //   </SecondaryButton>
  // );
};
