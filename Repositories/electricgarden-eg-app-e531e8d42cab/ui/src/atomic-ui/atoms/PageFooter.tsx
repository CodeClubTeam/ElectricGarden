import styled from 'styled-components/macro';
import banner from '../../static/repeating-banner-updated.svg';

export const PageFooter = styled.footer`
    background: repeat-x center url(${banner});
    min-height: 100px;
`;
