import styled from 'styled-components/macro';

import { PageHeader } from '../../../../atomic-ui';

export const LessonsPageHeader = styled(PageHeader)`
    h1 {
        max-width: var(--body-max-width);
        margin-left: auto;
        margin-right: auto;
    }
`;
