{
  "name": "catm1-receiver",
  "version": "1.0.0",
  "description": "Receives device readings via catm1 messages and sends into event hub",
  "main": "out/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build": "tsc --build"
  },
  "dependencies": {
    "raygun": "^0.12.2"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.2",
    "@types/raygun": "^0.11.0",
    "typescript": "^4.0.3"
  }
}
